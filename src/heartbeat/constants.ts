export const HEARTBEAT_FILENAME = 'HEARTBEAT.md';
export const HEARTBEAT_ACK_TOKEN = 'HEARTBEAT_OK';
export const DEFAULT_HEARTBEAT_EVERY = '30m';
export const DEFAULT_HEARTBEAT_PROMPT = `Read HEARTBEAT.md and run due tasks only. If nothing needs attention, reply ${HEARTBEAT_ACK_TOKEN}.`;

