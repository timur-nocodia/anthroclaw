# Timur Agent Lab Files

This directory is the default writable file-transfer sandbox for `timur_agent`.

Use it for small reversible file-transfer checks. Do not place provider secrets,
customer exports, or large generated artifacts here.
